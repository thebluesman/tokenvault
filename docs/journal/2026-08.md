# Project Journal — 2026-08

## 2026-08-31

- **[ops]** Initialized the git repo, pushed to `github.com/thebluesman/tokenvault`, and moved the original PRD draft into `docs/prd.md`. _Repo previously existed as an untracked local folder with just the PRD file._ → [docs/prd.md](../prd.md)
- **[process]** Scaffolded the Claude Code workspace: `@tech-lead`, `@product-manager`, `@ux-designer`, `@frontend-engineer` (dormant), and `@historian` agents, the `historian-check.py` Stop hook, and the `github-issues` skill — adapted from the pattern used in `horizon`. _Solo-contributor project needs the same doc-provenance and backlog conventions as other Claude Code–built repos, scaled to a flat backlog instead of sprint ceremony._ → [CLAUDE.md](../../CLAUDE.md)
- **[decision]** Adopted GitHub Issues (via `gh` CLI) as the backlog, no sprint ceremony. _Matches `horizon`'s ADR-0002 reasoning: solo contributor, already GitHub-hosted, no need for a second system of record._ → [docs/adr/0001-task-tracking-tooling.md](../adr/0001-task-tracking-tooling.md)
- **[decision]** Reframed Tokenvault's primary use case from write (token JSON → Figma) to read (Figma → token JSON). _Existing Figma files already have Variables/Styles defined directly in Figma; bringing them under git management should start by importing what's already there, not requiring manual token re-creation inside a plugin UI._ Restructured §6.5 into 6.5.1 Import (new, primary), 6.5.2 Apply, 6.5.3 Drift detection; re-sequenced Build Plan Phase 2 to focus on import + schema, pushing apply/drift to Phase 3. Updated §1, §2 (added "concrete driver" paragraph), §3, §7, §9, §10, §11 throughout. → [docs/prd.md](../prd.md)
- **[decision]** Split the import phase into two: Variables (Phase 2) and Styles (Phase 3). _Figma's API boundary cleanly separates Variables (collections/modes → token sets/themes, covering color/number/boolean/string types) from Styles (paint/text/effect/grid, covering typography/shadow/grid types); this mirrors the actual plugin architecture and makes the scope of each phase explicit._ Build Plan now has 9 phases; updated §6.5.1 Import with explicit Variables vs. Styles paths. → [docs/prd.md](../prd.md)
- **[decision]** Marked Phases 1–3 (Scaffold, Import — Variables, Import — Styles) as locked and Phases 4–9 (Local editor, Figma application, Git sync, Themes/aliasing/math, Export pipeline, Polish) as provisional in §9 Build Plan. _Phases 1–3 are scoped and ready to build; Phase 4–9 detail (editor UX, apply/drift semantics, sync flow) is deliberately deferred until real imported token data exists from Phases 2–3, rather than designing speculatively ahead of that._ → [docs/prd.md](../prd.md)
